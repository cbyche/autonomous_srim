# Autonomous S-RIM 요구사항 정의서

## 프로젝트 개요
S-RIM(잔여이익모델) 기반의 종목 분석 로직을 한국투자증권 Open API와 연동하여,
매수/매도 판단을 자동화하고 웹 대시보드로 포트폴리오를 모니터링하는 시스템.

## 핵심 기능

### 1. S-RIM 분석 엔진
- KRX 전 종목에 대한 S-RIM 분석 수행
- 4단계 가격 지표 계산: 매수가격, 적정가격, 매도가격, 최종가격
- BBB- 채권수익률 기반 요구수익률 산출
- FnGuide 재무제표 파싱을 통한 ROE/지배주주지분 추출
- 매수 후보 필터링 (현재가 < 매수가격 × 0.9 + 추가 조건)

### 2. 매매 전략
- **매수**: 현재가 < 매수가격 × 0.9 + 필터링 조건 충족 시
- **3단계 분할매도**:
  - 1단계 (적정가격 도달): 보유량의 1/3 매도
  - 2단계 (매도가격 도달): 보유량의 1/2 매도 (총 2/3 정리)
  - 3단계 (최종가격 도달): 보유량의 전량 매도
- **강제 매도**: 특정 조건 불만족 시 전량 매도

### 3. 스케줄링
- S-RIM 전체 분석: 매일 04:00 또는 매주 토요일 04:00 (설정 선택)
- 매도 조건 체크: 장중(09:00~15:30) 가변 주기 (5분/10분/1시간 등 설정 가능)

### 4. 알림 (텔레그램)
- 매수/매도 시그널 실시간 알림
- 두 가지 운영 모드:
  - 승인 모드: 사용자 승인 후 실행
  - 자동 모드: 자동 실행 후 결과 보고
- 텔레그램 커맨드를 통한 상태 조회 및 모드 전환

### 5. 웹 대시보드 (localhost:8000)
- 보유 종목 현황 (현재가 vs S-RIM 가격대 시각화)
- 매수 후보 종목 리스트
- 시그널/주문/알림 이력
- 설정 변경 UI (운영 모드, 체크 주기 등)

### 6. 한국투자증권 API 연동 (후순위)
- 현재가 조회, 잔고 조회
- 매수/매도 주문 실행
- 모의투자 환경 우선 개발 후 실전 전환

## 기술 스택
- Python 3.11+
- FastAPI (웹 서버)
- SQLite + SQLAlchemy (데이터베이스)
- APScheduler (스케줄링)
- python-telegram-bot (알림)
- pandas, numpy, numpy-financial (데이터 처리)
- BeautifulSoup4 (웹 스크래핑)
